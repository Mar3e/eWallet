import 'package:flutter/material.dart';

Widget addVerticalSpace(double pixels) {
  return SizedBox(
    height: pixels,
  );
}

Widget addHorizontalSpace(double pixels) {
  return SizedBox(
    width: pixels,
  );
}
